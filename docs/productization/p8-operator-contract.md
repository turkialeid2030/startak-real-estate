# P8 Operator Contract

An operator inspection is authorized only for a successfully authenticated verified identity holding the ADMIN role. Workspace scope must resolve through the authenticated canonical workspace and match the authenticated tenant. The operator surface remains read-only and fail-closed.
