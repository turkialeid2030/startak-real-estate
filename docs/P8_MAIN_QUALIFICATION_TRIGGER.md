# P8 Main Qualification Trigger

P7 is merged into `main`. This marker intentionally creates a fresh P8 head after retargeting to `main` so required release and trusted-governance checks execute against the current sequential base before merge.
