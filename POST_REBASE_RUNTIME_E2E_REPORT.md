# POST_REBASE_RUNTIME_E2E_REPORT

## Browser engine attempt
Attempted: `npx playwright install chromium`. Result: **FAILED with exact error**:
```
Error: Download failed: server returned code 403 body 'Host not in allowlist: cdn.playwright.dev. Add this host to your network egress settings to allow access.'
URL: https://cdn.playwright.dev/builds/cft/151.0.7922.34/linux64/chrome-linux64.zip
```
This is a hard network-egress restriction in this execution environment (allowlist covers npm/pypi/github registries only, not cdn.playwright.dev). The `@playwright/test` NPM package itself installed successfully (5 packages, from the allowed npm registry) — only the Chromium binary download failed. This blocker is environmental, not a code or configuration defect.

HEADLESS_BROWSER_AVAILABLE = FALSE
BROWSER_ENGINE = NONE (could not be installed)

Per this Wave's own Section 5 instruction, this SSR/build verification is NOT downgraded to be reported as "browser E2E" — it is reported honestly as what it is below.

## What WAS proven this pass (real execution, not simulation)
1. **Local Tailwind build**: `dist/assets/index-DWDdlU8g.css` (13.76 kB) generated by a real `npm run build`. `cdn.tailwindcss.com` reference count in built output = 0.
2. **Production build**: `npm run build` → exit 0, 2382 modules transformed, CSS+JS bundle produced.
3. **Production preview server**: `npm run preview` → real Vite preview server, `curl http://localhost:4173/` → HTTP 200, response HTML confirmed referencing the local built CSS asset (not the CDN).
4. **Full regression**: all 15 existing characterization/architecture test scripts still exit 0 after every runtime change.
5. Canonical source SHA256 unchanged throughout.

## What was NOT proven (honest gap — requires an actual browser)
All interactive E2E items (Sections 7-20 of this Wave's command) require a live browser executing real JavaScript with mouse/keyboard interaction: application boot visual check, Existing Building/Land+Development input editing and recalculation observation, financing toggle clicks, Saved Deals create/reload/load flow, Reset button click, sensitivity section visual inspection, responsive viewport rendering, browser console error capture. None of these were executed — no browser exists in this environment, and no substitute (SSR, jsdom, static inspection) is claimed as equivalent per this Wave's explicit instruction.

## Known runtime gaps
Same as REBASE_KNOWN_LIMITATIONS.md (DEF-001..004, COV-001/002, OBS-001, LIVE_CONNECTED_SOURCES=0, all future NOT_IMPLEMENTED capabilities) — unaffected by this pass, which touched only build tooling, never calculation logic.
