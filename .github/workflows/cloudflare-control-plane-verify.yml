name: Cloudflare Control Plane Verify

on:
  workflow_dispatch:
    inputs:
      expected_commit_sha:
        description: 'Exact 40-character main commit SHA whose production control plane is being verified'
        required: true
        type: string
  issue_comment:
    types: [created]

permissions:
  contents: read

concurrency:
  group: cloudflare-control-plane-verify
  cancel-in-progress: false

env:
  CURRENT_PRODUCTION_TRACKER: '396'

jobs:
  verify-cloudflare:
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'issue_comment' &&
       github.event.issue.number == 396 &&
       github.event.comment.author_association == 'OWNER' &&
       github.event.comment.user.login == github.repository_owner &&
       startsWith(github.event.comment.body, '/startak verify-control-plane '))
    name: cloudflare-control-plane
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 10
    env:
      CF_API_TOKEN_RAW: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CF_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
      CF_PROJECT_NAME: startak-real-estate
      CF_PRODUCTION_HOST: startak-real-estate.pages.dev
      CF_RATE_KV_NAME: startak-riai-rate-limit-prod
      CF_AUDIT_KV_NAME: startak-riai-audit-prod

    steps:
      - name: Bind verification to exact trusted main commit
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          DISPATCH_EXPECTED_SHA: ${{ inputs.expected_commit_sha }}
          COMMENT_BODY: ${{ github.event.comment.body }}
          COMMENT_ACTOR: ${{ github.event.comment.user.login }}
          REPOSITORY_OWNER: ${{ github.repository_owner }}
        run: |
          set -euo pipefail
          [[ "$GITHUB_REF" == 'refs/heads/main' ]] || { echo 'Production control-plane verification may only run from main.'; exit 1; }
          if [[ "$EVENT_NAME" == 'workflow_dispatch' ]]; then
            requested="$(printf '%s' "$DISPATCH_EXPECTED_SHA" | tr '[:upper:]' '[:lower:]')"
          else
            [[ "$COMMENT_ACTOR" == "$REPOSITORY_OWNER" ]] || { echo 'Owner-only control-plane verification command.'; exit 1; }
            [[ "$COMMENT_BODY" =~ ^/startak[[:space:]]verify-control-plane[[:space:]]([a-fA-F0-9]{40})[[:space:]]*$ ]] || { echo 'Malformed control-plane verification command.'; exit 1; }
            requested="$(printf '%s' "${BASH_REMATCH[1]}" | tr '[:upper:]' '[:lower:]')"
          fi
          [[ "$requested" =~ ^[a-f0-9]{40}$ ]] || { echo 'Expected commit SHA must be an exact 40-character SHA.'; exit 1; }
          current_main="$(git ls-remote "https://github.com/${GITHUB_REPOSITORY}.git" refs/heads/main | awk '{print $1}' | tr '[:upper:]' '[:lower:]')"
          [[ "$requested" == "$current_main" ]] || { echo "Trusted main changed: requested=$requested current=$current_main"; exit 1; }
          [[ "$requested" == "${GITHUB_SHA,,}" ]] || { echo "Expected SHA does not match trusted workflow SHA. expected=$requested workflow=$GITHUB_SHA"; exit 1; }
          printf '%s' "$requested" > /tmp/verified-main-sha
          echo "Control-plane verification bound to trusted main commit ${requested:0:12}."

      - name: Preflight repository configuration
        shell: bash
        run: |
          set -euo pipefail
          test -n "${CF_API_TOKEN_RAW:-}" || { echo 'CLOUDFLARE_API_TOKEN secret is missing'; exit 1; }
          test -n "${CF_ACCOUNT_ID:-}" || { echo 'CLOUDFLARE_ACCOUNT_ID variable is missing'; exit 1; }
          echo 'Repository Cloudflare credentials are configured.'

      - name: Normalize Cloudflare API token formatting
        shell: bash
        run: |
          set -euo pipefail
          raw="${CF_API_TOKEN_RAW:-}"
          token="${raw//$'\r'/}"
          token="${token//$'\n'/}"
          token="${token#"${token%%[![:space:]]*}"}"
          token="${token%"${token##*[![:space:]]}"}"

          case "$token" in
            Bearer\ *) token="${token#Bearer }" ;;
            bearer\ *) token="${token#bearer }" ;;
          esac

          token="${token#"${token%%[![:space:]]*}"}"
          token="${token%"${token##*[![:space:]]}"}"

          if [[ ${#token} -ge 2 && "${token:0:1}" == '"' && "${token: -1}" == '"' ]]; then
            token="${token:1:${#token}-2}"
          elif [[ ${#token} -ge 2 && "${token:0:1}" == "'" && "${token: -1}" == "'" ]]; then
            token="${token:1:${#token}-2}"
          fi

          token="${token#"${token%%[![:space:]]*}"}"
          token="${token%"${token##*[![:space:]]}"}"

          test -n "$token" || { echo 'CLOUDFLARE_API_TOKEN became empty after safe formatting normalization'; exit 1; }
          [[ "$token" != *[[:space:]]* ]] || { echo 'CLOUDFLARE_API_TOKEN contains internal whitespace and is malformed'; exit 1; }

          echo "::add-mask::$token"
          if [[ "$token" != "$raw" ]]; then
            echo 'Cloudflare API token formatting normalized without disclosure.'
          else
            echo 'Cloudflare API token formatting already canonical.'
          fi
          printf 'CF_API_TOKEN=%s\n' "$token" >> "$GITHUB_ENV"

      - name: Verify project-scoped API access
        shell: bash
        run: |
          set -euo pipefail
          test -n "${CF_API_TOKEN:-}" || { echo 'Normalized Cloudflare API token missing'; exit 1; }
          auth=(-H "Authorization: Bearer ${CF_API_TOKEN}" -H 'Content-Type: application/json')
          body="$(mktemp)"
          status="$(curl --silent --show-error -o "$body" -w '%{http_code}' "${auth[@]}" "https://api.cloudflare.com/client/v4/accounts/${CF_ACCOUNT_ID}/pages/projects/${CF_PROJECT_NAME}" || true)"
          if [[ "$status" != '200' ]]; then
            error_code="$(jq -r '.errors[0].code // empty' "$body" 2>/dev/null || true)"
            echo "Cloudflare Pages project access failed: HTTP ${status}${error_code:+ code=${error_code}}"
            rm -f "$body"
            exit 1
          fi
          project_json="$(cat "$body")"
          rm -f "$body"
          jq -e '.success == true' <<<"$project_json" >/dev/null
          jq -e '.result.production_branch == "main"' <<<"$project_json" >/dev/null
          printf '%s' "$project_json" > /tmp/cf-project.json
          echo 'Cloudflare project-scoped bearer access is valid for the STARTAK Pages project.'

      - name: Verify production bindings and secret presence
        shell: bash
        run: |
          set -euo pipefail
          project_json="$(cat /tmp/cf-project.json)"
          jq -e '.result.deployment_configs.production.kv_namespaces.RIAI_RATE_LIMIT_KV != null' <<<"$project_json" >/dev/null || { echo 'Missing production binding: RIAI_RATE_LIMIT_KV'; exit 1; }
          jq -e '.result.deployment_configs.production.kv_namespaces.RIAI_AUDIT_KV != null' <<<"$project_json" >/dev/null || { echo 'Missing production binding: RIAI_AUDIT_KV'; exit 1; }
          jq -e '.result.deployment_configs.production.env_vars.RIAI_AUDIT_SUBJECT_SALT != null' <<<"$project_json" >/dev/null || { echo 'Missing production secret: RIAI_AUDIT_SUBJECT_SALT'; exit 1; }
          echo 'Required Wave C Pages bindings are present.'

      - name: Verify governed AI activation variables
        shell: bash
        run: |
          set -euo pipefail
          project_json="$(cat /tmp/cf-project.json)"
          missing=()
          require_env() {
            local key="$1"
            if ! jq -e --arg key "$key" '.result.deployment_configs.production.env_vars[$key] != null' <<<"$project_json" >/dev/null; then missing+=("$key"); fi
          }
          require_env RIAI_AI_PROVIDER_PROTOCOL
          require_env RIAI_AI_PROVIDER_URL
          require_env RIAI_AI_ALLOWED_HOSTS
          require_env RIAI_AI_PROVIDER_KEY
          require_env RIAI_AI_MODEL
          require_env RIAI_AI_GLOBAL_TOKEN_BUDGET_PER_DAY
          provider_protocol="$(jq -r '.result.deployment_configs.production.env_vars.RIAI_AI_PROVIDER_PROTOCOL.value // empty' <<<"$project_json")"
          if [[ -n "$provider_protocol" && "$provider_protocol" != 'CHAT_COMPLETIONS' && "$provider_protocol" != 'OPENAI_RESPONSES' ]]; then
            echo 'Invalid RIAI_AI_PROVIDER_PROTOCOL; allowed values are CHAT_COMPLETIONS or OPENAI_RESPONSES.'; exit 1
          fi
          public_enabled="$(jq -r '.result.deployment_configs.production.env_vars.RIAI_PUBLIC_AI_ENABLED.value // "false"' <<<"$project_json" | tr '[:upper:]' '[:lower:]')"
          if [[ "$public_enabled" == 'true' ]]; then
            require_env RIAI_TURNSTILE_SITE_KEY
            require_env RIAI_TURNSTILE_SECRET_KEY
            echo 'AI activation mode detected: public Turnstile.'
          else
            require_env RIAI_AI_ACCESS_ISSUER
            require_env RIAI_AI_ACCESS_AUD
            echo 'AI activation mode detected: Cloudflare Access.'
          fi
          if (( ${#missing[@]} > 0 )); then
            echo 'Missing production AI configuration keys:'
            printf ' - %s\n' "${missing[@]}"
            exit 1
          fi
          echo "Provider protocol validated: ${provider_protocol}."
          echo 'Required governed AI activation variables are present; values and secrets were not printed.'

      - name: Verify KV namespaces
        shell: bash
        run: |
          set -euo pipefail
          auth=(-H "Authorization: Bearer ${CF_API_TOKEN}" -H 'Content-Type: application/json')
          kv_json="$(curl -fsS "${auth[@]}" "https://api.cloudflare.com/client/v4/accounts/${CF_ACCOUNT_ID}/storage/kv/namespaces?per_page=1000")"
          jq -e '.success == true' <<<"$kv_json" >/dev/null
          jq -e --arg n "$CF_RATE_KV_NAME" '.result | any(.title == $n)' <<<"$kv_json" >/dev/null || { echo "KV namespace not found: $CF_RATE_KV_NAME"; exit 1; }
          jq -e --arg n "$CF_AUDIT_KV_NAME" '.result | any(.title == $n)' <<<"$kv_json" >/dev/null || { echo "KV namespace not found: $CF_AUDIT_KV_NAME"; exit 1; }
          echo 'Required KV namespaces exist.'

      - name: Verify exact production Access application and audience
        shell: bash
        run: |
          set -euo pipefail
          auth=(-H "Authorization: Bearer ${CF_API_TOKEN}" -H 'Content-Type: application/json')
          access_json="$(curl -fsS "${auth[@]}" "https://api.cloudflare.com/client/v4/accounts/${CF_ACCOUNT_ID}/access/apps?per_page=1000")"
          jq -e '.success == true' <<<"$access_json" >/dev/null
          access_app="$(jq -c --arg host "$CF_PRODUCTION_HOST" '.result | map(select((.domain? == $host) or ((.destinations? // []) | any(((.uri? // .hostname? // .host? // "") == $host) or ((.uri? // "") == ("https://" + $host)) or ((.uri? // "") == ("https://" + $host + "/")))))) | first // empty' <<<"$access_json")"
          test -n "$access_app" || { echo "Exact production Access application not found for ${CF_PRODUCTION_HOST}."; exit 1; }
          jq -e '(.type == "self_hosted") and ((.aud? // "") | length > 0)' <<<"$access_app" >/dev/null || { echo 'Production Access application is missing a self-hosted type or audience tag.'; exit 1; }
          echo 'Exact production Access application and audience tag are present.'

      - name: Verify production deployment is the exact authorized main commit
        shell: bash
        run: |
          set -euo pipefail
          auth=(-H "Authorization: Bearer ${CF_API_TOKEN}" -H 'Content-Type: application/json')
          expected="$(cat /tmp/verified-main-sha)"
          matched='false'
          for attempt in $(seq 1 30); do
            deployments_json="$(curl -fsS "${auth[@]}" "https://api.cloudflare.com/client/v4/accounts/${CF_ACCOUNT_ID}/pages/projects/${CF_PROJECT_NAME}/deployments?env=production&per_page=1")"
            jq -e '.success == true' <<<"$deployments_json" >/dev/null
            deployed="$(jq -r '.result[0].source.config.commit_hash // .result[0].deployment_trigger.metadata.commit_hash // empty' <<<"$deployments_json" | tr '[:upper:]' '[:lower:]')"
            stage="$(jq -r '.result[0].latest_stage.status // empty' <<<"$deployments_json")"
            if [[ "$deployed" == "$expected" && "$stage" == 'success' ]]; then matched='true'; break; fi
            sleep 10
          done
          [[ "$matched" == 'true' ]] || { echo "Production deployment did not reach success for exact authorized commit $expected within the verification window."; exit 1; }
          echo "Production deployment matches ${expected:0:12} and is successful."

      - name: Publish verification summary
        if: always()
        shell: bash
        run: |
          {
            echo '## Cloudflare Control Plane Verification'
            echo
            echo "- Project: \`${CF_PROJECT_NAME}\`"
            echo "- Production host: \`${CF_PRODUCTION_HOST}\`"
            echo "- GitHub event: \`${GITHUB_EVENT_NAME}\`"
            echo "- Commit: \`${GITHUB_SHA}\`"
            echo "- Production tracker: \`#${CURRENT_PRODUCTION_TRACKER}\`"
            echo '- Verification is exact-SHA bound and may be initiated only by trusted main workflow_dispatch or an owner-only command on the production tracker.'
            echo '- No pull_request, pull_request_target, or automatic push path receives the production Cloudflare credential.'
            echo '- Required Wave C bindings and governed AI activation configuration are verified fail-closed.'
            echo '- Cloudflare token formatting is normalized without disclosure before API access.'
            echo '- Project-scoped bearer access is used as the authoritative credential preflight.'
            echo '- API token value is never printed.'
          } >> "$GITHUB_STEP_SUMMARY"
